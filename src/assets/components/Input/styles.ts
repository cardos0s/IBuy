import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#FFF',
        paddingHorizontal: 16,
        height: 48,
        borderRadius: 6,
        borderWidth: 1,
        borderColor: '#E6E6E6',
        width: '100%',
    },
});